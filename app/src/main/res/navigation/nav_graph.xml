<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/searchFragment">

    <fragment
        android:id="@+id/searchFragment"
        android:name="com.example.weatherapp.ui.search.SearchFragment"
        android:label="SearchFragment"
        tools:layout="@layout/search_fragment">
        <action
            android:id="@+id/action_searchFragment_to_currentWeatherFragment"
            app:destination="@id/currentWeatherFragment" />
    </fragment>

    <fragment
        android:id="@+id/currentWeatherFragment"
        android:name="com.example.weatherapp.ui.current.CityFragment"
        android:label="CurrentWeatherFragment"
        tools:layout="@layout/city_weather_fragment">
        <argument
            android:name="measure"
            app:argType="string"
            android:defaultValue="metric" />
    </fragment>
    <fragment
        android:id="@+id/savedLocationsFragment"
        android:name="com.example.weatherapp.ui.saved.SavedLocationsFragment"
        android:label="SavedLocationsFragment" >
        <action
            android:id="@+id/action_savedLocationsFragment_to_currentWeatherFragment"
            app:destination="@id/currentWeatherFragment" />
    </fragment>
    <action android:id="@+id/action_global_savedLocationsFragment" app:destination="@id/savedLocationsFragment" />

</navigation>